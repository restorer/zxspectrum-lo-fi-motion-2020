    module math

;-----------------------------------------------------------------------------------------------------------------------

; OUT:
;   a - next random number
; USE:
;   hl
random
    ld hl,.seed : ld a,(hl)
    dup 7 : inc l : add a,(hl) : ld (hl),a : edup
    ld (.seed),a
    ret

    org (($ + 7) / 8) * 8

.seed
    ;   12345678
    db "TGT_TEAM"

;-----------------------------------------------------------------------------------------------------------------------

    endmodule
